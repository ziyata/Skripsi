<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Skripsi">
        <activity
            android:name=".ui.transaksi.TransaksiDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.pilihBarang.PilihBarangActivity"
            android:exported="false" />
        <activity
            android:name=".ui.qr.QrActivity"
            android:exported="false" />
        <activity
            android:name=".ui.prediksi.PrediksiActivity"
            android:exported="false" />
        <activity
            android:name=".ui.pembelian.PembelianActivity"
            android:exported="false" />
        <activity
            android:name=".ui.barang.BarangActivity"
            android:exported="false" />
        <activity
            android:name=".ui.checkout.CheckoutActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>